#!/bin/sh
set -e

# first arg is `-f` or `--some-option`
if [ "${1#-}" != "$1" ]; then
	set -- php-fpm "$@"
fi

exec "$@"

# ??
# ${1#-} ??????? # ?????????#??????
# set -- ????--??????????????????????????????????????"-"??????? $1 $2 ??
# exec ?????  ????????????????????  test.sh ls / ???????????????? $@ ?????????
